apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "golang-crud.fullname" . }}-test-connection"
  labels:
    {{- include "golang-crud.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "golang-crud.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
